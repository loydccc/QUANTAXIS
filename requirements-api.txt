# Minimal dependencies to run the local QUANTAXIS Product API (api/app.py)
# Keeps installs lightweight and avoids heavy/optional research stack.

fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# used by signals generation helpers
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# parquet writing for backtest artifacts (equity_curve.parquet, etc.)
pyarrow>=14.0.0
